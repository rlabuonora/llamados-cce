<td><%= proposal.proveedor %></td>
<td><%= link_to("Evaluar", new_proposal_evaluation_path(proposal), class: 'btn btn-primary') %></td>
<td><%= link_to 'Eliminar', call_proposal_path(@call, proposal), method: :delete, class: 'btn btn-primary' %></td>
